# GRABIX ships a bundled Python 3.11 runtime.
# We build PyO3 against the Python 3.11 stable ABI in Cargo.toml, so this
# release build must not float to a newer machine-local Python ABI.
[env]
PYO3_USE_ABI3_FORWARD_COMPATIBILITY = "1"

[target.x86_64-pc-windows-msvc]
rustflags = []
